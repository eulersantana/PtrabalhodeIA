# gerador de estado

def geraEstados(estadoAtual,movimento,tabuleiro):
	novoComponentes = []
	for pos in tabuleiro:		
		if estadoAtual.componentes[0].posicao.x -1 == pos.posicao.x  and if estadoAtual.componentes[0].posicao.y -1 == pos.posicao.y 
			if len(novo)

		


